/* eslint-disable linebreak-style */
/* eslint-disable no-unused-vars */
class Books {
  constructor(author, title, books, button) {
    this.author = author;
    this.title = title;
    this.books = books;
    this.button = button;
  }
}